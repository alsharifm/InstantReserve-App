To run application, unzip folder and open folder through visual studio

in terminal run "py echo-server.py"

in a separate terminal run "py echo-client.py"

follow in-terminal instructions to send a message to the server